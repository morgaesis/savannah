#!/bin/bash
# Pre-commit hook: scan staged files for secrets
echo "Scanning for secrets..."
infisical scan --no-git 2>&1 | grep -v "A new release of infisical"
if [ ${PIPESTATUS[0]} -ne 0 ]; then
    echo "SECRET DETECTED - commit blocked. Fix the leak before committing."
    exit 1
fi
